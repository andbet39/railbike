<h1>Mygroup#index</h1>
<p>Find me in app/views/mygroup/index.html.erb</p>

<ul>
  <% @groups.each() do |group| %>
      <li><%= link_to group.name , {:controller => "mygroup", :action => "show", :id => group.id} %></li>
  <% end %>
</ul>

